<% provide(:title, @user.name) %>
<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
        <h1>
            <%= gravatar_for @user %>
            <%= @user.name %>
        </h1>
        </section>
    </aside>
</div>

User created at: <%= @user.created_at %><br>
User updated at: <%= @user.updated_at %><br>
Current time is: <%= Time.now.strftime("%I:%M %p") %>